# aantekening (application)

The Flutter application shell: window, navigation panes, page editor and
settings. It contains no domain logic — that lives in the packages it depends
on.

See the [repository README](../../README.md) and
[`docs/architecture.md`](../../docs/architecture.md).
