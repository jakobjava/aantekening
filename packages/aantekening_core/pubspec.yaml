name: aantekening_core
description: Pure-Dart domain model, page document format and serialization for aantekening.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
