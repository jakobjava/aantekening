name: aantekening_math
description: Math input translation (linear to LaTeX) and formula rendering for aantekening.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0
  flutter: ">=3.27.0"

dependencies:
  aantekening_core: ^0.1.0
  flutter:
    sdk: flutter
  flutter_math_fork: ^0.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
