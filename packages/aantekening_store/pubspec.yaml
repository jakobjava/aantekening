name: aantekening_store
description: SQLite-backed storage, full-text search and asset management for aantekening.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  aantekening_core: ^0.1.0
  crypto: ^3.0.7
  path: ^1.9.1
  sqlite3: ^3.6.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
