name: aantekening_ai
description: Local AI model interfaces (Ollama, llama.cpp) for aantekening.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.9.0

dependencies:
  aantekening_core: ^0.1.0
  http: ^1.6.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
